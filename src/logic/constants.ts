export const TICKS_PER_SECOND = 30;
export const NIGHT_DURATION = 240 * TICKS_PER_SECOND;
export const MAX_POWER = NIGHT_DURATION * 2;

export const BONNIE_TIMER = Math.floor(4.25 * TICKS_PER_SECOND);
export const CHICA_TIMER = Math.floor(4.59 * TICKS_PER_SECOND);
export const FREDDY_TIMER = Math.floor(6.13 * TICKS_PER_SECOND);
export const FOXY_TIMER = Math.floor(5.13 * TICKS_PER_SECOND);
export const FOXY_TRAVEL_TIME = 3 * TICKS_PER_SECOND;
export const FOXY_HALL_TIMEOUT = 10 * TICKS_PER_SECOND;
